package entity

/**
 * Enum to distinguish between the four player colors:
 * blue, yellow, red, green and default (representing an empty field).
 */
enum class PlayerColor {
    BLUE,
    YELLOW,
    RED,
    GREEN,
    BLANK,
    ;
}